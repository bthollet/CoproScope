# QA live 8766 - 2026-05-21

## Perimetre

Recette focalisee et non destructive sur la livraison live:

- URL: `http://127.0.0.1:8766/?token=local-secret`
- Captures: `docs/assets/ux-livraison-reelle-2026-05-21-8766/`
- Fichier de test ajoute: `server/tests/test_ui_live_ux_contract.py`
- Methode: requetes HTTP `GET` uniquement, sans POST, sans modification des donnees applicatives.

## Contrat automatise

Le test `LiveUiUxContractTests` verifie:

- les routes live primaires repondent `200` et ne rendent pas une page 404;
- le contenu utile de chaque page est present dans `main#contenu`;
- le contenu utile reste proche du debut du contenu HTML initial;
- le bandeau contexte, quand il precede le contenu, reste borne et ne masque pas la page utile;
- les libelles novices attendus sont presents sur cockpit, ajout document, demandes, AG/contentieux et depot;
- les routes P1, relance syndic et pieces manquantes exposent une action utile;
- les racines `raw`, `restricted`, `logs` et `private` ne sont pas servies et ne fuitent pas dans les reponses.

## Routes verifiees

Routes primaires:

- `/`
- `/actions`
- `/comptes`
- `/documents`
- `/documents/ajouter`
- `/pieces`
- `/demandes`
- `/ag-contentieux`
- `/gouvernance`
- `/pilotage`
- `/confidentialite`
- `/chantiers`
- `/depot`

Routes d'action ciblees:

- `/actions?priority=P1`
- `/actions?scope=syndic&tab=relance`
- `/pieces?proof=missing`

Sondes privees attendues en 404:

- `/raw/ui-live-probe.txt`
- `/restricted/ui-live-probe.txt`
- `/logs/ui-live-probe.log`
- `/private/ui-live-probe.txt`
- `/exports/raw/ui-live-probe.txt`
- `/exports/restricted/ui-live-probe.txt`
- `/exports/logs/ui-live-probe.log`
- `/exports/private/ui-live-probe.txt`

## Execution

Commande tentee:

```powershell
python -m pytest server/tests/test_ui_live_ux_contract.py -q
```

Resultat:

```text
No module named pytest
```

Commande:

```powershell
python -m unittest discover -s server/tests -p test_ui_live_ux_contract.py -v
```

Resultat du 2026-05-21:

```text
Ran 5 tests in 2.557s
OK
```

## Verdict

GO QA live sur le perimetre teste: les routes principales repondent, les pages exposent un contenu utile et des actions lisibles, le bandeau contexte ne prend pas le dessus sur le contenu, et les racines privees testees restent non servies.
