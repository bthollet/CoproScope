# Plan de nuit - `RM-2026-0091`, run sans Brice

Chantier: `CH-20260907-002116-RM-2026-0091-liste-legale-et-veille-continue`.
Conversation: `CONV-2026-2144`. Ecrit le 2026-09-07 a 01h, avant depart de Brice.

## 1. Le mecanisme, dit franchement

Il n'y a **ni cron, ni heartbeat, ni relance automatique**. Le fil pilote
travaille dans ce meme tour jusqu'a ce que le lot soit fini ou qu'une condition
d'arret tombe. C'est ce que la doctrine du depot autorise - un travail borne a
un `CH-*` deja declare - et rien de plus: aucun nouveau `ORD-*` ne sera choisi
seul, aucune tache du gouvernail hors de ce chantier ne sera ouverte.

Si tout est fini avant le reveil, le fil s'arrete et laisse son `BOT-END`. Il ne
va pas chercher du travail ailleurs.

## 2. Ce que je NE ferai pas cette nuit

Ce sont les gardes, et elles comptent plus que la liste des taches.

| Interdit | Pourquoi |
|---|---|
| **Pousser vers `origin`** | Le coordinateur integre, une branche a la fois, apres relecture. Pousser 10 commits non relus pendant que tu dors inverse cet ordre. La branche reste locale. |
| Toucher `_comptes_extraction_*` | Tu as tranche: lot separe `RM-2026-0094`. Ces fichiers n'existent meme pas dans ce worktree. |
| Ouvrir `RM-2026-0094` ou `RM-2026-0095` | Inscrits au gouvernail, pas developpes. |
| Lancer un serveur, ouvrir un port | Aucun besoin, et un serveur durable non surveille est exactement ce que la doctrine interdit. |
| Toucher une instance | Aucune donnee reelle n'entre dans ce lot. |
| Trancher les arbitrages B et C | Integration CoproScope et second editeur: ils demandent ta decision. |
| Modifier un fichier hors de mon ownership declare | Notamment `server/tests/test_ui_systeme_design_contraste.py`, qui appartient a un autre lot. |

## 3. Conditions d'arret - je me gare et j'attends

- un test echoue et je ne sais pas dire pourquoi apres **deux** tentatives;
- une correction exigerait de refactorer un fichier que je ne possede pas;
- le garde-fou des 600 lignes force une extraction hors de mon perimetre;
- un arbitrage non pris apparait;
- un verificateur adverse (voir §5) confirme qu'un de mes constats est faux et
  que la correction depasse le lot.

Dans tous ces cas: statut `EN_ATTENTE_USER` dans `docs/presence_agents.md`, avec
la question posee en clair. Pas de contournement, pas de devinette.

## 4. Le travail, en cinq vagues

Chaque vague suit le meme cycle: **j'ecris -> je teste -> un sous-agent essaie
de me refuter -> je corrige ou je defends -> je commite**.

### Vague 1 - le bandeau dans la page *(en cours)*

`annoncer()` emploie encore `CS_VEILLE.manques`, le comptage par rubrique
d'editeur. Les attendus etant desormais indexes par obligation, **le bandeau ne
trouverait plus rien**. Il bascule sur `manquesLegaux`, affiche le type d'espace
detecte comme tu l'as demande, et devient enfin testable: le mini-DOM sait
maintenant fabriquer des elements et attacher une ombre, donc le code du bandeau
s'execute au lieu de sortir par sa propre garde.

Fichiers: `lecteur.js`, `tests/executer.mjs`, `server/tests/test_extension_bandeau.py`.
Preuve: le bandeau nomme l'obligation et pas seulement le code, et il echoue
franchement si le DOM manque au lieu de se taire.

### Vague 2 - la veille continue, option A2 *(ta demande principale)*

Minuterie dans le service worker via `chrome.alarms`, plus la permission. Les
**trois gardes sont obligatoires**, pas optionnelles:

1. reconnaitre la page de connexion, et enregistrer `NON_EXPLORE` au lieu de
   conclure. Sans elle, une session expiree produit *tout a disparu* - le pire
   constat que l'outil sache rendre, sur une simple panne d'authentification;
2. afficher la derniere **tentative** et le dernier **succes**. Un ecran qui ne
   montre que le succes laisse croire que rien n'a change alors que plus rien
   n'est observe depuis trois semaines;
3. presence seulement. Le contenu reste sur geste humain.

Fichiers: `manifest.json`, `fond.js`, `veille.js`, `popup.js`, `popup.html`,
`test_extension_veille.py`, `test_extension_plomberie.py`.

### Vague 3 - `RUBRIQUE_ECHEC`, un vrai defaut

Aujourd'hui un bloc present mais illisible ressort `PARCOURUE` avec zero piece,
c'est-a-dire un **faux "rubrique vide"** - donc une fabrique de faux retraits,
exactement ce que tout le lot cherche a eviter.

Axe: la presence de la charpente de liste. Invariant: une rubrique servie et
vide affiche quand meme sa charpente - un tableau, un en-tete, ou un message
*aucun document*. Code: bloc sans ligne **et** sans texte -> `ECHEC` avec motif;
bloc avec texte mais sans ligne -> `PARCOURUE`, zero piece, motif nomme.

### Vague 4 - code mort, reserves, hygiene

| Element | Sort |
|---|---|
| `fusionner_couverture` | conservee, documentee comme reservee a `RM-2026-0092`, avec un test qui prouve qu'elle n'a aucun appelant |
| `lignes_sans_facture` | **branchee**: c'est le seul manque que l'observation sait produire sans attendu declare |
| `lire_depenses` | conservee, et son role d'etalon nomme dans la docstring - sinon le prochain qui passe la supprime |
| `CLOTURES` / `ETATS_RUBRIQUE` | employes comme garde de validation a l'ecriture |
| `CONTENU_VERIFIE` | reserve **ecrite**, plus un test qui prouve qu'aucun producteur ne l'ecrit |
| permission `tabs` | **pas ajoutee** - elle fonctionne par les `host_permissions`, et demander un acces inutile est le defaut que la plomberie existe pour empecher. Le mecanisme est ecrit en commentaire et le test le nomme |
| `docs/_lecture_extranet/` | supprime de l'arbre principal |

### Vague 5 - README, trace, comptages

Le README de l'extension est en 0.3.0 et ne mentionne ni veille, ni manque, ni
bandeau. Reecrit. Puis le `BOT-END` complet: fichiers modifies, fichiers evites,
comptage des 600 lignes, nombre de tests **avec la preuve que Node a tourne**,
limites, et la section AXE / invariant / hors valeurs observees.

## 5. La strategie de sous-agents, et pourquoi elle est ce qu'elle est

### Les ecritures ne se deleguent pas

La regle du depot est explicite: **un agent = un worktree**. Cinq sous-agents
qui ecriraient dans `coproscope-extranet-conformite-20260904` se marcheraient
dessus, et tu te reveillerais devant un arbre incoherent.

Leur donner chacun un worktree isole est possible, mais fusionner cinq branches
sans toi pendant la nuit ajoute un risque pour un gain faible: **le goulot n'est
pas la frappe, c'est le jugement**. Les vagues 1 a 5 se touchent d'ailleurs sur
`manifest.json`, `veille.js` et `_extranet_schema.py`.

**Decision: toutes les ecritures passent par le fil pilote, en serie.**

### Les sous-agents servent a la confiance, pas au debit

Ils tournent en parallele, en **lecture seule**, et font le travail qui
serialiserait sinon derriere moi.

| Role | Mission | Quand |
|---|---|---|
| **Verificateur adverse** | Recevoir mon message de commit et **essayer de le refuter**. Par defaut il conclut *faux*; il ne valide que ce qu'il a mesure lui-meme. | un par vague |
| **Epreuve second corpus** | La conception tient-elle sur `instances/erables_pseudo_test`, le second cabinet ? La regle du depot l'exige pour tout lot d'extraction. | une fois, vague 3 |
| **Novice / QA** | Lire l'ecran livre et le brouillon, et dire si quelqu'un qui decouvre le sujet comprend. GO / NO-GO trace. | une fois, apres vague 2 |
| **Relecteur de doctrine** | Verifier que rien dans le lot n'emet un verdict de conformite, ne capture le contenu de `EXT-C-04`, ni ne sort vers le syndic. | une fois, avant le `BOT-END` |

### Pourquoi le verificateur adverse, precisement

Parce que **j'ai eu tort deux fois cette nuit, et de la meme facon**: j'ai
affirme une suite verte en lisant le code de sortie de `tail` au bout d'un
tuyau, et j'ai affirme qu'aucune prise stable n'existait alors que ma propre
sonde suivante l'a trouvee. Les deux etaient des affirmations non mesurees.

La garde n'est donc pas *"faire attention"*, c'est structurel: chaque constat
que je publie est soumis a quelqu'un dont le travail est de le casser.

## 6. Discipline de preuve, appliquee sans exception

- tout code de sortie est lu avec `CODE=${PIPESTATUS[0]}`, **jamais** au bout
  d'un tuyau;
- tout compte de tests dit si Node a tourne - sans lui, 22 tests du lot
  disparaissent en silence;
- les trois echecs et l'erreur preexistants sont rejoues **seuls** pour prouver
  que mes ajouts ne les ont pas causes;
- aucun BOM ajoute ni retire: seuls les deux registres en portent un;
- le garde-fou des 600 lignes est relance a chaque commit.

## 7. Ce que tu trouveras au reveil

Dans tous les cas: une branche locale, non poussee, avec des commits qui se
lisent un par un; `docs/presence_agents.md` a jour avec le statut reel; et un
`BOT-END` qui dit ce qui est fait, ce qui ne l'est pas, et pourquoi.

Si je me suis gare, la question qui m'a arrete sera la premiere ligne du
`BOT-END`, en clair.
