# Finition - zone vocabulaire

Branche : `chantier/fix-vocabulaire`

Verdict du verificateur independant : **incomplet**

## Ce que le verificateur a etabli

VERIFICATION FAITE SUR LA BRANCHE chantier/fix-vocabulaire, COMMIT cc7b609 (le worktree annonce, coproscope-fix-vocabulaire-20260904, n'existe pas; j'ai reconstruit deux arbres detaches, AVANT=9c6c5b1 et APRES=cc7b609, et force PYTHONPATH - le piege du .pth qu'il signale est reel et confirme: le .pth de server/.venv pointe sur le src de l'arbre principal).

POINT 1 - LES TESTS ECHOUENT-ILS SUR LE CODE D'AVANT: OUI, largement. J'ai copie test_vocabulaires_partages.py tel quel dans un arbre a 9c6c5b1 et l'ai lance contre le src d'avant. Resultat: 19 tests, 9 echecs et 8 erreurs. Cinq seulement passent, et ce sont exactement les cinq qui correspondent aux constats qu'il declare REFUTES ou hors zone (le pont de majorite, la matrice des retraits, le producteur de portee, les codes de constat affiches). Une sixieme classe, VocabulaireDesVuesTests, ne peut meme pas s'importer sur le code d'avant (V.IMPUTATIONS n'existe pas): elle ne prouve donc pas un defaut mesure, c'est un garde-fou neuf - a lire comme tel, pas comme un test de non-regression.

POINT 2 - LES PREUVES D'ORIGINE SE REPRODUISENT-ELLES ENCORE APRES CORRECTION: NON, sur les quatre que j'ai rejouees moi-meme, meme script sur les deux arbres.
- C009: AVANT, deux regles a egalite de score avec priorites 100 et 115 rendent Convocation_AG, score 5 (la priorite ne pese rien). APRES: PV_AG, score 120. Et les deux orthographes du meme reglage rendaient des scores differents (5 contre 45) avant, identiques apres. Corrige, pas cosmetique.
- C030: AVANT, portees_soumises('AVIS_CS ') rend 11 portees, 'avis_cs' 11, '' 11, comme un nom totalement inconnu; controle_applicable rend True et motif_hors_controle la chaine vide. APRES: ValueError nommant la liste attendue, dans les trois fonctions. Et le nom correct rend toujours ses 3 portees - la garde ne deplace pas la reponse juste.
- C031/C063/C093: AVANT, le releve des noms reellement passes a _cellule et portees_soumises donne six controles consultes sur sept, orphelin = MAJORITE, et 8 des 11 portees de MAJORITE absentes du SQL du constat. APRES: sept sur sept, zero orphelin, zero portee absente.
- C084: AVANT, _a_valider_seuil n'existe pas et le test de l'ecran est `confiance not in ('haute','')`, donc 'forte' -> incertain=True et '' -> incertain=False. APRES: 'forte' -> aucun avertissement, 'moyenne'/'faible' -> l'avertissement en nommant le niveau, '' -> une phrase propre ("l'outil n'a pas note avec quelle assurance..."). Trois etats, trois phrases.

POINT 3 - LES REFUTATIONS SONT-ELLES FONDEES: OUI, verifiees sur le chemin d'ecriture, pas sur la declaration.
- C021/C022/C061 (moitie majorite): majorite_lue('') rend bien 'NON_ENONCEE' DEJA sur le code d'avant, et surtout la fonction est reellement appelee a l'ecriture (_pont_actes_lignes.py:169 la pose sur majorite_annoncee). La preuve d'origine ne se reproduit plus. Refutation fondee.
- C056 (partie desaccord de cles): portee_resolution traduit bien les qualifications en portees des actes, et elle est appelee par le pont a deux endroits (_pont_actes_source.py:262 et 329). Elle retombe toujours sur une portee declaree. Refutation fondee.

SUITE COMPLETE, RELANCEE PAR MOI depuis le worktree APRES avec PYTHONPATH force: 1353 tests, 3 echecs et 1 erreur - exactement les quatre preexistants (test_drive_key_transfer, deux test_ui_drive_oauth, test_ui_messages_entrants). Aucun echec ajoute. Le compte de 1353 = 1333 de reference + 20 nouveaux est exact.

POURQUOI PAS `solide`: deux replis muets NEUFS, mesures, que rien ne signale et qu'il ne mentionne pas (detailles dans regressions_suspectes), plus un constat non traite et non declare dans son propre fichier (corrections_manquees). Aucun des deux replis ne change une sortie sur les donnees d'aujourd'hui - c'est pourquoi ce n'est pas `a_refaire`: le travail est reel, teste, et ne casse rien. Mais tous deux appartiennent a la classe exacte que ce lot existe pour fermer, et l'un d'eux arme un cran de plus un constat BLOQUANT deja ecrit dans l'audit. Deux patchs courts suffiraient.

DEBORDEMENT DE ZONE: _docuscope_parts/02_classification.py est le fichier principal de la zone classement (C010, C011, C019, C049, C053, C058, C075, C100, C101 y vivent tous) et une branche chantier/fix-classement existe, encore a 9c6c5b1. Il y a ecrit 97 lignes pour C009. Il l'a signale, c'est honnete, mais le coordinateur doit integrer ces deux lots dans l'ordre et remesurer: rallumer les priorites deplace des egalites, l'audit chiffrait l'effet a 148 lignes sur 3447. Les deux fichiers web/ touches sont justifies par C084 et la moitie de C094 qu'il revendique. Rien d'autre ne deborde.

## Ce qui manque (3)

- C064 - 'L'invariant une portee inconnue garde tous ses controles est faux pour la seule portee inconnue qui existe, et le test le contourne'. Le constat porte sur _actes_typologie.py, le fichier qu'il a possede et modifie pour C030, et sur test_resolutions_typage.py. Il se reproduit tel quel sur la branche: HORS_CONTROLE['ORDINAIRE'] vaut toujours {'RAPPORT_CS'} alors que le commentaire de fin de HORS_CONTROLE annonce qu'ORDINAIRE n'a aucune exclusion, et le test cense garder l'invariant interroge toujours la chaine 'PORTEE_INVENTEE' (test_resolutions_typage.py:70-71), une valeur que le code n'ecrit jamais, au lieu d'ORDINAIRE qui est le repli reel. C'est le meme genre d'ecart commentaire/code/test que C091, qu'il a corrige a deux lignes de la. Il ne le declare ni corrige, ni refute, ni hors portee.

- C017 - 'Une portee inconnue n'active pas tous les controles, elle les desactive tous'. Meme silence. Ce n'etait peut-etre pas dans sa liste, mais sa correction de C031 l'aggrave (voir regressions), ce qui aurait du le lui faire nommer au moins comme signalement au coordinateur, au meme titre que C072.

- SIGNALEMENT, PAS UN REPROCHE - C013 ('DEVENUE' ecrit par le producteur, absent des libelles, des trois compteurs et de la liste a relire cote ecran) est litteralement le meme motif que C084 sur une autre couture. S'il n'appartient a aucune zone attribuee, il restera ouvert: qu'il soit route explicitement vers la zone extraction/resolutions plutot que laisse entre deux lots.

## Regressions relevees (2)

- MESUREE - Le bornage de MAJORITE_NON_ENONCEE par `a.portee IN (les 11 portees)` eteint le constat sur un acte dont la portee n'est pas renseignee. Reproduction: deux actes identiques a majorite_annoncee='NON_ENONCEE', l'un portee='ORDINAIRE', l'autre portee=''. AVANT v_constats rend les deux; APRES il n'en rend qu'un, sans un mot. Le vide est explicitement admis a l'ecriture - la docstring de _valider_vocabulaire ecrit 'le vide est admis: une colonne non renseignee est un fait, pas une faute' - donc la valeur est atteignable. Et cela contredit l'invariant que le meme lot vient de documenter dans controle_applicable ('une portee inconnue rend True: on ne desactive pas un controle sur la foi d'une valeur qu'on ne sait pas lire'). C'est le constat C017, BLOQUANT, qu'il n'a mentionne ni comme sien ni comme hors portee: il vient de l'armer sur un septieme controle. Attenuation: le pont ecrit toujours une portee declaree (portee_resolution retombe sur ORDINAIRE), donc zero effet sur les donnees d'aujourd'hui. Correctif suggere: borner par `portee NOT IN (les portees retirees)` plutot que par `IN (les portees soumises)`, ce qui rend le doute au controle au lieu de le rendre au classement sans suite. Fichier: server/src/coproscope/modules/_actes_constats.py

- MESUREE - Dans _classify, `rule["lot"]` est devenu `_champ_regle(rule, "lot", "A_CLASSER")`. Une regle de taxonomie sans cle `lot` levait KeyError; elle rend desormais ('A_CLASSER', 'PV_AG', 5) - un type documentaire affirme, range dans un lot invente, sans erreur. Et `_verifier_regles`, la garde ajoutee juste au-dessus, ne rattrape pas ce cas: elle refuse une cle INCONNUE, jamais une cle REQUISE ABSENTE (verifie: elle accepte la regle sans `lot`). Un repli muet a donc ete cree dans la fonction meme qu'on durcissait. Zero effet aujourd'hui puisque le fichier livre porte `lot` sur ses 38 regles - sinon le code d'avant aurait plante. Correctif suggere: faire exiger `lot` et `document_type` par _verifier_regles. Fichier: server/src/coproscope/modules/_docuscope_parts/02_classification.py

## Ce que le correcteur affirmait avoir fait

- corriges : C009 - le bareme de classement lisait `priority`, `taxonomy.default.yml` ecrit `priorite` sur ses 38 regles. Reproduit: `_classify` avec deux regles a egalite de score et des priorites 100/115 rendait Convocation_AG (la premiere ecrite) au lieu de PV_AG. Corrige par une table declaree `CHAMPS_REGLE` (nom canonique -> ses deux orthographes), `_champ_regle` qui la lit, et `_verifier_regles`/`_verifier_taxonomie` qui refusent au chargement une cle que personne ne lit. Fichier: server/src/coproscope/modules/_docuscope_parts/02_classification.py | C030 - une faute de frappe d'un caractere sur un nom de controle rallumait le controle sur les onze portees. Reproduit: `portees_soumises('AVIS_CS ')` rendait 11 portees, comme un nom totalement inconnu, la ou le nom juste en rend 3. Corrige par `_exiger_controle`, appele par `controle_applicable`, `motif_hors_controle` et `portees_soumises`: un nom hors `CONTROLES` leve et dit ce qu'il attendait. La dissymetrie est conservee et documentee - une portee inconnue garde tous ses controles (elle vient de la donnee lue), un nom de controle inconnu leve (il vient du code). Ajout d'une verification a l'import: toute cle de `HORS_CONTROLE` est une portee declaree, tout retrait porte un controle declare. Fichier: server/src/coproscope/modules/_actes_typologie.py | C031 (= C063 = C093) - le controle MAJORITE etait declare parmi les sept, exclu portee par portee dans les motifs, et applique nulle part: ni cellule de matrice, ni borne de constat. Reproduit par relevé statique des noms passes a `_cellule(...)` et `portees_soumises(...)` dans les vues et les constats: MAJORITE etait le seul orphelin. Corrige en bornant le constat MAJORITE_NON_ENONCEE par `portees_soumises(CTRL_MAJORITE)`, comme ACTE_SANS_EXECUTION l'est par CTRL_EXECUTION. Les onze portees y sont soumises aujourd'hui, donc aucun constat ne change; ce qui change est qu'un retrait ecrit dans HORS_CONTROLE aura desormais un effet observable. Fichier: server/src/coproscope/modules/_actes_constats.py | C084 - l'ecran des seuils testait `confiance not in ('haute', '')`; les producteurs (`_resolutions_extraction._confiance`, le pont) n'ecrivent que `forte`, `moyenne`, `faible`. Reproduit: confiance 'forte' rendait incertain=True (l'avertissement du montant mal lu sur le seuil le mieux lu) et confiance '' rendait incertain=False (un montant dont la confiance n'a jamais ete lue passait pour confirme). Corrige par un vocabulaire declare `CONFIANCES` dans _actes_vocabulaire, valide a l'ecriture dans `_actes_store.ecrire`, et un `_a_valider_seuil` qui rend trois phrases distinctes pour trois etats - dont une phrase propre a la confiance non renseignee. Trois fixtures de test qui portaient le mot fantome 'haute' sont alignees sur le vocabulaire des producteurs. Fichiers: _actes_vocabulaire.py, _actes_store.py, actes_autorisation.py, web/controle_gouvernance_view.py, tests/test_actes_autorisation.py, tests/test_actes_generalisabilite.py, tests/test_controle_gouvernance.py | C094 (moitie) - `_fonde` appelait `_cellule(..., 'RESOLUTION', ...)` avec un nom qui n'est pas dans `CONTROLES`, donc interrogeait la matrice des retraits avec une cle inconnue. Corrige: le texte de la resolution n'est pas un controle, il passe desormais une chaine vide et `_cellule` documente ce que cela signifie. Sans ce changement, la garde de C030 aurait fait lever cet appel. Fichier: server/src/coproscope/web/_controle_gouvernance_source.py | Ajout structurel: `IMPUTATIONS` n'existait pas comme liste fermee (quatre constantes isolees, rien a quoi comparer une cinquieme valeur). La liste est declaree, ce qui rend la colonne verifiable par le test generique.

- refutes : C021 et C022 - le producteur ecrivait la chaine vide, le consommateur cherchait `NON_ENONCEE`, donc 0 constat la ou l'etalon en etablit 1. La preuve ne se reproduit plus sur la branche: `_pont_actes_source.majorite_lue` (introduite par le lot pont registre -> actes, commit 024e165) traduit `''` en `MAJORITE_NON_ENONCEE` avant l'ecriture, et sa docstring cite ce constat comme motif. Mesure: `majorite_lue('')` rend 'NON_ENONCEE'. Non corrige donc, mais verrouille: deux tests tiennent les deux bouts ensemble, et le litteral 'NON_ENONCEE' du SQL est remplace par la constante du vocabulaire pour qu'une lettre de moins ne puisse plus rendre zero constat en silence. | C061 (moitie majorite) - meme cause que C021/C022, meme refutation. La seconde moitie du constat (SANS_ISSUE_TRACEE ne vise aucun constat) n'est pas un desaccord de vocabulaire mais un constat qui n'existe pas: hors zone, voir ci-dessous. | C056 (partie 'les deux vocabulaires ne coincident pas, SEUIL_CONSULTATION_CS doit etre replie a la main sur SEUIL, rien dans le code ne le fait') - ne se reproduit plus: `_resolutions_qualification.portee_resolution` traduit les qualifications en portees du vocabulaire des actes (SEUIL_CONSULTATION_CS et SEUIL_MISE_EN_CONCURRENCE -> PORTEE_SEUIL), et `_pont_actes_source` l'appelle sur le segment du document. Un test generique verifie desormais que cette fonction ne rend que des portees declarees. Le reste de C056 - le taux de typage reel sur le corpus - est une mesure de donnees, pas un desaccord de cles.

- hors portee : C072 - `_decompte_voix.REGIMES` ne connait que 24, 25, 25-1, 26 alors que l'extracteur peut produire 23, 24-1, 26-1, et le corpus porte 'article 25B'. C'est bien la meme classe, mais le module appartient a la voie decompte des voix (branche dediee) et la bonne cle n'est pas une question d'orthographe: elle demande un arbitrage juridique sur le regime de l'article 25 b, qui n'est pas le mien. Signale au coordinateur pour eviter une correction concurrente avec C076. | C066 et la moitie SANS_ISSUE_TRACEE de C061 - il ne s'agit pas d'une valeur cherchee au mauvais nom mais d'un constat qui n'existe pas et d'une cellule qui n'a pas d'etat propre. Ecrire ce constat appartient a la zone constats. | C088 - deux constats reels (PV_SANS_DATE_LUE) sont produits et routes vers aucune pastille. C'est le sens inverse de mon garde-fou: mon test verifie que tout code affiche a un producteur, pas que tout code produit est affiche. Le rendre symetrique aurait fait echouer la suite sur un defaut qui appartient a la zone ecran; je l'ai donc laisse ouvert plutot que de le contourner. | C094 (reste) - `FORCE_VERS_STATUT.get(valeur, 'manquante')` transforme toute valeur du mauvais vocabulaire en piece manquante plausible, et trois vocabulaires du meme schema partagent des valeurs (PORTEES n IMPUTATIONS, FORCES n TVA_REGIMES). Le piege est pose, pas arme: les colonnes concernees vivent sur des tables differentes. Le rendre bruyant touche le rendu de l'ecran, zone front. | C010, C011, C019, C049, C053 - defauts du classifieur et de la taxonomie qui ne sont pas des desaccords de cles (accents non neutralises, signature de titre cablee en aval, motif de nom de fichier trop large, deux registres concurrents). Ma correction de C009 rallume les priorites, ce qui deplace des egalites; le lot classement devra remesurer les 148 lignes annoncees par l'audit.

- tests : "Suite complete depuis server/ : 1353 tests, 3 echecs et 1 erreur - exactement les preexistants (test_drive_key_transfer, deux test_ui_drive_oauth, test_ui_messages_entrants). Aucun echec ajoute. Les 1353 comprennent les 20 tests du nouveau fichier server/tests/test_vocabulaires_partages.py. Chaque correction a d'abord ete reproduite sur la source d'avant (export de HEAD dans un dossier de travail, meme script de mesure lance sur les deux arbres) : AVANT -> C009 rend Convocation_AG, portees_soumises('AVIS_CS ') rend 11 portees sans erreur, MAJORITE est declare sans consommateur, confiance 'for
