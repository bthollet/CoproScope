# Finition - zone registre

Branche : `chantier/fix-registre`

Verdict du verificateur independant : **incomplet**

## Ce que le verificateur a etabli

VERIFICATION FAITE SUR LA BRANCHE chantier/fix-registre, COMMIT 789c539 (worktree reel: C:\Users\brice\CoproScope\coproscope\.claude\worktrees\wf_28fb808e-096-6 ; le chemin dev\worktrees\coproscope-fix-registre-20260904 annonce dans la commande n'existe pas). Arbre propre, un seul commit.

POINT 1 - LES TESTS ECHOUENT-ILS SUR LE CODE D'AVANT ? OUI, verifie independamment.
J'ai reconstruit un arbre neutre (src de 9c6c5b1 + tests de 789c539) et lance les six classes nouvelles: 24 tests, 20 en echec sur le code d'avant (7 FAIL + 13 ERROR), 4 qui passent des deux cotes. Les 4 sont des gardes anti-sur-correction, pas les seules preuves d'une correction: test_deux_annees_differentes_rendent_les_memes_lignes (caracterise le fait que `year` ne filtre pas, et ne doit pas filtrer), test_un_proces_verbal_qui_vote_sur_une_demande_reste_un_proces_verbal, test_un_type_reconnu_ne_porte_aucun_motif, test_une_colonne_vide_n_ecrit_pas_un_zero. Chaque correction revendiquee porte au moins un test qui tombe sur le code d'avant. Ce point tient.

POINT 2 - LES PREUVES D'ORIGINE SE REPRODUISENT-ELLES ENCORE ? NON, sur les trois que j'ai rejouees moi-meme, hors des tests du correcteur.
- C044: deux lignes de meme cle (resolution_id, etat, origine) + une troisieme distincte soumises a remplacer_pour_documents -> retour 2, et `select count(*)` sur la base rend 2. Avant: retour 3 pour 2 stockees. collisions_de_cle nomme la cle ecrasee. Corrige, non cosmetique.
- C055: quatre projets « Resolution n°11 / 11.1 / 11.2 / 11.6 » -> 4 resolution_id distincts (R011, R011.01, R011.02, R011.06) la ou le code d'avant en rendait UN. Chaine complete verifiee: acte_id_resolution prenait deja `sous_numero`, RESOLUTION_FIELDS le porte, ensure_schema ajoute la colonne a une table existante par ALTER TABLE.
- C045: 154 actes de portee ORDINAIRE, table liens_gouvernance vide -> `a_demander = 0` (avant: 154), `a_outiller = 154` compte a part, aucune pastille n'affirme plus d'exigibilite. Controle positif fait: avec un acte REJETEE dans le lot, a_demander remonte a 1 - la correction ne rend pas zero par construction.
Ce point tient.

POINT 3 - LES REFUTATIONS SONT-ELLES FONDEES ? OUI pour C056.
Verifie ligne a ligne: _resolutions_qualification.portee_resolution produit bien les cinq portees dites sans producteur (AUTORISATION_COPROPRIETAIRE l.491, DESIGNATION_ORGANE l.497 et 507, DESIGNATION_SYNDIC l.502, MODALITES l.526, ENGAGEMENT_DEPENSE l.532) et replie SEUIL_CONSULTATION_CS / SEUIL_MISE_EN_CONCURRENCE sur SEUIL l.482. Elle EST appelee a l'ecriture: _pont_actes_source l.310 et l.377, puis ligne_acte ecrit candidat.portee. La these « aucun producteur / typage applique nulle part » ne tient pas sur le code actuel. Le residu retenu (le silence sur la RAISON du non-typage) est le bon residu.

SUITE COMPLETE: 1357 tests, 3 echecs + 1 erreur = exactement la base preexistante (test_drive_key_transfer, deux test_ui_drive_oauth, test_ui_messages_entrants). 1333 + 24 = 1357, aucun echec ajoute. Garde-fou 600 lignes: test_code_line_limit OK, aucun fichier au-dessus (test_pont_actes 503, le nouveau fichier 140).

PRIVACY: scan du diff complet - aucun nom, tantieme, vote nominatif ni chemin local ajoute.

POURQUOI PAS `solide`: j'ai reproduit une regression introduite par la nouvelle nature DEMANDE_INSCRIPTION (detail en regressions_suspectes). Elle fait DISPARAITRE un proces-verbal entier du registre, ce qui est exactement la famille de defaut que ce lot combat. Elle se corrige en un caractere de regex.
POURQUOI PAS `a_refaire`: aucune correction n'est cosmetique, les points 1 et 2 tiennent fermement, et redemander le lot serait disproportionne. Le lot est mergeable APRES le correctif de regex et l'arbitrage sur C054.

## Ce qui manque (3)

- C054 - la moitie affichable reste a faire: nommer les copies concurrentes d'une meme assemblee SUR L'ECRAN, pas seulement dans le resume de run. Le refus d'elire la copie qui fait foi est fonde et demande un arbitrage; l'absence totale de signalement a l'utilisateur ne l'est pas.

- Rien d'autre de la zone n'est laisse sans raison. C051 (derives du registre) et C053 (registre orphelin non declare) sont correctement renvoyes hors zone - j'ai verifie le point qui les rend inoffensifs ici: le pont ne lit que les documents dont le doc_id porte une ligne de resolution, donc aucun derive n'entre dans le modele des actes par ce chemin. C015 (le PV etalon eclate sur quatre types) n'est pas traite mais releve du meme classifieur, hors zone, et sa consequence sur la gouvernance est neutralisee par la meme raison.

- Couverture partielle de C055: SOUS_NUMERO_RE ne reconnait que la forme « Resolution n°X.Y ». Un cabinet qui numeroterait « 11.1 - » en tete de ligne (forme NUMERO_RE_B_LIGNE) reproduirait la collision. C'est la forme mesuree sur le second corpus qui est couverte, donc la limite est acceptable, mais elle n'est pas ecrite dans le code.

## Regressions relevees (5)

- REGRESSION REPRODUITE, bloquante avant merge - un proces-verbal peut etre entierement ecarte du registre. `TITRE_PV_RE = r"(?i)proc[eè]s[-\s]?verbal"` (_resolutions_registre.py) n'admet QU'UN seul separateur. Un PV titre « PROCES - VERBAL DE L'ASSEMBLEE GENERALE » (espace-tiret-espace, forme frequente en OCR de titre espace) dont la tete de 600 caracteres mentionne une demande d'inscription a l'ordre du jour est classe DEMANDE_INSCRIPTION et `continue` dans build_register: toutes ses resolutions disparaissent. Mesure faite par moi: ce meme texte rend PV_AG sur 9c6c5b1 et DEMANDE_INSCRIPTION sur 789c539. Aggravant: le doc_id ecarte va dans `demandes_inscription_ecartees`, mais `run.log_run` reste au niveau OK - seule une collision de cle fait passer en WARN (ligne `run.log_run("WARN" if ecrasees else "OK", ...)`). Une perte de PV est donc journalisee au niveau OK, et le resume ne remonte a aucun ecran. Correctif: `[-\s]*` ou `[\s-]{0,3}` dans TITRE_PV_RE, et faire passer le niveau en WARN des qu'une demande est ecartee. Aucun test ne couvre ce cas.

- C054 nomme mais invisible - `assemblees` et `assemblees_sans_date_lue` n'existent que dans le dict de resume de pont_actes.verser, donc dans le journal de run. L'ecran de controle continue d'afficher trois fois la meme resolution n°7 avec deux issues contradictoires sans jamais dire que c'est le meme document, ce qui est la consequence litterale du constat. Le refus d'elire la copie qui fait foi est justifie (regle de preuve, pas de transport), mais la partie « le dire a l'utilisateur » reste non faite.

- Durabilite de la pastille `avis_manquant` en SOURCE_OUTIL - verifie: seul le chemin convocation (`avis_cs_affirme`) ecrit un lien REL_AVIS_CS (_pont_actes_lignes.py l.454-469), donc le classement en SOURCE_OUTIL est exact AUJOURD'HUI. Mais rien n'oblige a le rebasculer le jour ou une resolution recevra un lien AVIS_CS: un avis reellement manquant serait alors range a vie sous « ce que l'outil n'a pas etabli » et sortirait des points a instruire. Meme remarque pour `acte_sans_execution`, qui est un vrai constat de v_constats desormais exclu de l'arithmetique. A tracer.

- Cosmetique, sans consequence mesuree: pont_actes.verser lit le registre documentaire deux fois (registre_documents en tete, puis _documents_par_id via candidats_resolutions) - 3 447 lignes de CSV parcourues deux fois. Et sur le corpus de repro, l'ecran affiche « 154 lignes concernees » (outil) et « 154 decisions » (ce qui ne demande rien) dans deux blocs voisins avec deux sens differents.

- Chevauchement de fichiers a arbitrer par le coordinateur, pas une faute du correcteur: _actes_schema.py et _pont_actes_lignes.py et controle_gouvernance_view.py sont aussi modifies par worktree-wf_28fb808e-096-1 ; controle_gouvernance_view.py et test_controle_gouvernance.py par worktree-wf_28fb808e-096-2 ; _resolutions_extraction.py et _resolutions_motifs.py par worktree-wf_28fb808e-096-4 (decompte des voix). Conflits textuels certains sur controle_gouvernance_view.py et test_controle_gouvernance.py. Le correcteur a en revanche correctement laisse intact le `except sqlite3.OperationalError: return []` de gouvernance_store.lire (C014/C079, autre zone) et l'a signale.

## Ce que le correcteur affirmait avoir fait

- corriges : C045 (PRIORITE, le chiffre annonce a Brice) - REPRODUIT: 154 actes de portee ORDINAIRE, table `liens_gouvernance` vide, `v_constats` rend 0 ligne, et l'ecran affichait `154 points a instruire sur 154 decisions lues`. La pastille `avis_manquant` portait le chiffre ET affirmait un fondement de droit, « la consultation prealable est exigible sur ce type », sur des actes dont le type n'a justement pas ete reconnu - alors qu'aucun code du produit n'ecrit un lien AVIS_CS pour une resolution d'assemblee (seule une convocation qui affirme la consultation en pose un: grep sur REL_AVIS_CS le montre). CORRIGE: chaque constat declare sa source, SOURCE_PIECES ou SOURCE_OUTIL. `a_demander`, `arithmetique`, le lead et le drapeau `rompu` ne comptent que les pieces; l'outil est compte a part sous « Ce que l'outil n'a pas encore etabli ». `avis_manquant` passe en SOURCE_OUTIL et n'affirme plus d'exigibilite. Nouvelle pastille `type_non_reconnu` qui nomme les 154 pour ce qu'elles sont. Le chiffre de 44 sur 141 annonce a Brice etait donc du meme moule: il additionnait des cellules vides de l'outil et des constats reels. Apres correction, sur le corpus de repro: 0 point a instruire, 154 lignes que l'outil ne sait pas renseigner. Tests: LeChiffreDuTitreTests (4 tests). | Parametre `year` de l'ecran - REPRODUIT: memes 154 lignes pour year=2024 et year=2026, sous les titres « Decisions et marches de l'exercice 2024 » et « ... 2026 ». CORRIGE en refusant le faux remede: `year` ne DOIT pas filtrer, `liens_seuil` a ete ecrit pour qu'un seuil vote en 2024 pour 24 mois arme le controle d'une depense de 2026 sans qu'aucune requete ne prenne d'annee. Ce qui mentait, c'etait le titre. Il ne revendique plus d'exercice; la bande d'identite affiche l'exercice courant de l'application ET les exercices que les lignes portent, plus une phrase de couverture qui compte les actes sans exercice lisible. Tests: ExerciceAfficheTests (3 tests). | C044 - REPRODUIT isolement: deux lignes de meme cle (resolution_id, etat, origine) soumises a `remplacer_pour_documents` rendaient 2, la base en contenait 1. CORRIGE: le retour est le nombre STOCKE. Nouvelle `collisions_de_cle` qui NOMME la cle ecrasee au lieu de la compter. `build_register` rapporte `resolutions` (stockees), `resolutions_construites` et `resolutions_ecrasees`, et journalise en WARN quand il y a une collision. Tests: ComptageStockeTests (2 tests sur 3). | C055 - REPRODUIT: quatre projets « Resolution n°11 / 11.1 / 11.2 / 11.6 » rendaient UN seul resolution_id; trois disparaissaient dans INSERT OR REPLACE sans erreur. CORRIGE: `SOUS_NUMERO_RE` lue dans la TETE du segment (fenetre de 120 caracteres, pour qu'un `n°11.4` cite dans le corps ne renumerote pas le point courant), colonne `sous_numero` ajoutee a RESOLUTION_FIELDS, et le degre entre dans `resolution_id`. Le pont lisait deja `ligne.get('sous_numero')`: la chaine est complete de bout en bout sans le modifier. Tests: SousNumeroTests (3 tests). | C016 - REPRODUIT par lecture: `_decompte` CONSOMMAIT voix_pour/voix_contre/voix_abstention/base_voix pour produire une part, et `lignes_attributs` ne versait que les valeurs derivees. Apres versement, le modele portait le pourcentage et plus les voix: le fait etalon (4 899 presents sur 10 000, article 25 hors d'atteinte) redevenait irrecalculable. CORRIGE: les voix enoncees sont versees en attributs de provenance SYNDIC_AFFIRME (voix_pour, voix_contre, voix_abstention, denominateur_imprime, voix_relevees_brutes), a cote des valeurs COPROSCOPE_CALCULE. `passerelle_citee` (renseignee 173/173, aucune destination) est versee et ne se confond pas avec `passerelle_utilisee`. Une colonne vide n'ecrit rien - fabriquer un 0 transformerait « le PV ne chiffre pas » en « personne n'a vote pour ». Tests: VoixVerseesTests (3 tests). | C052 - REPRODUIT dans le pont: `_documents_par_id` faisait `{doc.get('doc_id'): doc for doc in docs}`, donc la DERNIERE ligne du CSV gagnait. Sur un registre ou 528 lignes partagent leur doc_id, quand deux lignes divergent sur `text_path` c'est le texte relu - donc la portee de TOUTES les resolutions du document - qui se decidait sur l'ordre des lignes d'un fichier. CORRIGE: `registre_documents` retient la premiere ligne qui porte un `text_path` (critere de contenu, pas d'ordre) et NOMME les doc_id dont les lignes divergent sur un champ decisif; `verser` les remonte dans `doc_ids_ambigus` et journalise en WARN. Tests: RegistreDocumentaireTests (3 tests). | nature_assemblee acceptant une demande d'inscription - REPRODUIT: une demande d'inscription de cinq questions qui redige le texte propose au vote, formule de cloture comprise, rend PV_AG, cinq resolutions, cinq ADOPTEE, versees en etat CONSTATEE. CORRIGE selon la regle d'axe du depot: l'axe est la maniere dont une piece d'assemblee se NOMME en tete (decret 67-223 art. 17 pour le PV, art. 9 et 11 pour la convocation, art. 10 pour la demande d'inscription); l'invariant est que le corps peut CITER n'importe quelle formule mais que la tete dit ce que la piece EST. Nouvelle nature DEMANDE_INSCRIPTION, ecartee de build_register et nommee dans le resume (`demandes_inscription_ecartees`). La fenetre est bornee a 600 caracteres et un titre `proces-verbal` en tete prime, pour que le PV qui MET AUX VOIX une demande d'inscription - cas reel - reste un PV. Hors des valeurs observees: un cabinet qui ne se declare pas en tete retombe sur la regle des clotures en serie, degradation identique a aujourd'hui. Tests: DemandeInscriptionTests (2 tests). | C056, residu reel - la portee ORDINAIRE etait MUETTE. `portee_resolution` calcule ses `indices` (corps non relu / periode d'exercice illisible / marche sans montant) et le pont les jetait: trois causes tres differentes, un seul mot « Type non reconnu » a l'ecran. CORRIGE: attribut `portee_non_reconnue_motif` verse pour tout acte ORDINAIRE, avec un motif nomme (MOTIF_AUCUNE_REGLE) quand aucune regle ne s'est declenchee - jamais un blanc. Tests: 3 tests dans VoixVerseesTests.

- refutes : C056, sa these principale - « cinq portees du modele n'ont aucun producteur » et « le typage est disponible et applique nulle part a l'ecriture ». REFUTE sur le code actuel. `_resolutions_qualification.portee_resolution` produit les onze portees, y compris les cinq dites sans producteur: DESIGNATION_SYNDIC (l.502), DESIGNATION_ORGANE (l.497 et 507), ENGAGEMENT_DEPENSE (l.532), AUTORISATION_COPROPRIETAIRE (l.491) et MODALITES (l.526). Et elle EST appelee a l'ecriture: `_pont_actes_source.candidats_resolutions` l'appelle par resolution et `ligne_acte` ecrit `candidat.portee`. La mesure de l'audit (154 ORDINAIRE, 7 APPROBATION_COMPTES, 6 BUDGET, 6 SEUIL) reproduit exactement la colonne `qualifications` du registre, donc elle vient d'une replique de versement qui lisait cette colonne - chemin que le pont n'emprunte pas. La these du vocabulaire non replie (SEUIL_CONSULTATION_CS / SEUIL_MISE_EN_CONCURRENCE a replier sur SEUIL « a la main ») tombe pour la meme raison: le repli est fait dans portee_resolution l.483. Ce qui survivait du constat est le silence sur la RAISON du non-typage, corrige ci-dessus.

- hors portee : C054 (trois copies de la meme assemblee, et la seule datee contredit l'etalon) - la moitie structurelle est reelle et je l'ai NOMMEE sans la trancher: le resume de versement rend maintenant `assemblees` (actes et documents par ag_id) et `assemblees_sans_date_lue`, et journalise en WARN. Je n'ai pas elu la copie qui fait foi: c'est une regle de preuve, pas une decision de transport, et l'audit dit lui-meme que le choix par defaut serait le mauvais (la copie datee annonce 38 adoptees la ou le PV en porte 39). Cela demande un arbitrage. La moitie mesuree - « la copie datee est celle qui contredit l'etalon » - n'est pas re-verifiable dans mon cadre: elle exige l'instance reelle, que je ne dois pas ecrire, et que le CLAUDE.md declare non utilisable comme etalon. | C051 (69,3 % des lignes du registre sont des derives, dont 400 portent un type) - la cause est dans la PRODUCTION du registre documentaire, c'est-a-dire l'inventaire et le classifieur `02_classification.py`, explicitement exclus de ma zone; le consommateur fautif est l'ecran documents qui compte par type, pas la chaine de gouvernance. Verifie au passage: le pont ne lit que les documents dont le doc_id porte une ligne de resolution, donc aucun derive n'entre dans le modele des actes par ce chemin. Rien a corriger ici sans empieter. | C053 (deux registres documentaires concurrents dans l'instance) - `instance.register('documents')` resout un chemin DECLARE dans instance.yml; le registre orphelin est un fichier non declare ailleurs dans l'instance. Le detecter suppose un balayage du systeme de fichiers de l'instance, donc une capacite nouvelle dans `core/_common_parts/01_io_and_instance_config.py` (hors de ma zone), et les instances me sont interdites en ecriture. C'est un chantier de configuration d'instance, pas un defaut de code de gouvernance.

- tests : Suite complete: 1357 tests, 3 echecs + 1 erreur = exactement la base preexistante (test_drive_key_transfer, deux test_ui_drive_oauth, test_ui_messages_entrants). Aucun echec ajoute. 24 tests nouveaux, tous verifies en echec sur le code d'avant par `git stash push -- server/src` puis relance ciblee: 4 echecs + 9 erreurs sur les classes nouvelles, 0 apres restauration. Le garde-fou des 600 lignes repasse (test_pont_actes.py etait monte a 621, scinde en test_pont_actes_faits_verses.py).
